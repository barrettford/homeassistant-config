# This file needs to begin indented 2 spaces

# Deadbolts
  lock.back_deadbolt_locked:
    refresh_value: true
    delay: 2
  lock.front_deadbolt_locked:
    refresh_value: true
    delay: 2
  lock.garage_deadbolt_locked:
    refresh_value: true
    delay: 2
  lock.kitchen_deadbolt_locked:
    refresh_value: true
    delay: 2

# # phone batteries
# sensor.barretts_iphone_battery:
#   icon: mdi:battery-70
# sensor.ericas_iphone_battery:
#   icon: mdi:battery-70
